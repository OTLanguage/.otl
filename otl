#!/bin/sh

path=`pwd`
delete='uninstall'
cd ${HOME}/.otl

if [ -z $1 ]; then
	java Main "${path}"
	cd $path
	exit 0
else
	if [ ${1} == ${delete} ] ; then
		sed -i '' '/alias otl="sh ${HOME}/d' ~/.zshrc
		cd ${HOME}
		rm -rf .otl
		exit 0
	else
		java Main "${path}" $1
		cd $path
		exit 0
	fi
fi